%%% -*- erlang -*-
[
 {riak_shell, [
              {logging, off},
              {logfile, "{{runner_log_dir}}/riak_shell"},
              {cookie, riak},
              {show_connection_status, @STATUS@},
              {nodes, [
@NODES@
                      ]}
             ]}
].

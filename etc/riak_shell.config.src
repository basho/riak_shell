%%% -*- erlang -*-
[
 {riak_shell, [
              {blob_line_len, 20},
              {blob_max_len, 100},
              {logging, off},
              {cookie, riak},
              {show_connection_status, false},
              {nodes, [
@NODES@
                      ]}
             ]}
].
